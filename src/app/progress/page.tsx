'use client'

export default function ProgressPage() {
  return (
    <main className="min-h-screen pb-20 px-4 pt-12" style={{ background: 'var(--bg)' }}>
      <h1 className="text-2xl font-bold mb-2" style={{ color: 'var(--text-primary)' }}>Progress</h1>
      <p className="text-sm" style={{ color: 'var(--text-muted)' }}>Coming in Phase 3</p>
    </main>
  )
}
